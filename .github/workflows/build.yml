name: Build YTFlags
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
   
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build YTFlags
    runs-on: macos-latest
    permissions:
      contents: write
    
    steps:
      - name: Install dependencies
        run: brew install make ldid

      - name: Set PATH environment variables
        run: |
          echo "$(brew --prefix make)/libexec/gnubin" >> $GITHUB_PATH
          echo "THEOS=${{ github.workspace }}/theos" >> $GITHUB_ENV

      - name: Clone Theos
        run: git clone --quiet --depth=1 --recurse-submodules https://github.com/roothide/theos.git
           
      - name: Download iOS SDK
        run: |
          git clone --quiet --depth=1 -n --filter=tree:0 https://github.com/Tonwalter888/iOS-SDKs.git
          cd iOS-SDKs
          git sparse-checkout set --no-cone iPhoneOS18.6.sdk
          git checkout
          mv *.sdk $THEOS/sdks

      - name: Clone YTFlags
        run: git clone --quiet --depth=1 https://github.com/Tonwalter888/YTFlags.git

      - name: Clone YouTubeHeader
        run: git clone --quiet --depth=1 https://github.com/PoomSmart/YouTubeHeader.git $THEOS/include/YouTubeHeader

      - name: Clone PSHeader
        run: git clone --quiet --depth=1 https://github.com/PoomSmart/PSheader.git $THEOS/include/PSHeader

      - name: Build YTFlags
        run: |
          cd YTFlags
          echo "YTFLAGS_VERSION=$(grep '^Version:' control | cut -d ' ' -f2)" >> $GITHUB_ENV
          make clean package DEBUG=0 FINALPACKAGE=1
          make clean package DEBUG=0 FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless
          make clean package DEBUG=0 FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=roothide
          mv packages/*.deb ${{ github.workspace }}

      - name: Upload the tweak .deb(s)
        uses: actions/upload-artifact@v7
        with:
         name: YTFlags v${{ env.YTFLAGS_VERSION }}
         path: ${{ github.workspace }}/*.deb
         if-no-files-found: error
         overwrite: true

      - name: Create a draft release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ env.YTFLAGS_VERSION }}
          name: v${{ env.YTFLAGS_VERSION }} 
          files: |
             dev.water888.ytflags_${{ env.YTFLAGS_VERSION }}_iphoneos-arm.deb
             dev.water888.ytflags_${{ env.YTFLAGS_VERSION }}_iphoneos-arm64.deb
             dev.water888.ytflags_${{ env.YTFLAGS_VERSION }}_iphoneos-arm64e.deb
          draft: true
